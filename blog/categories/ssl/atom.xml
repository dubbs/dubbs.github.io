<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Ssl | Kevin Warrington]]></title>
  <link href="http://dubbs.github.io/blog/categories/ssl/atom.xml" rel="self"/>
  <link href="http://dubbs.github.io/"/>
  <updated>2015-07-03T11:21:06-06:00</updated>
  <id>http://dubbs.github.io/</id>
  <author>
    <name><![CDATA[Kevin Warrington]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[SSL Certificate for Localhost]]></title>
    <link href="http://dubbs.github.io/blog/2014/09/24/ssl-certificate-for-localhost/"/>
    <updated>2014-09-24T22:22:09-06:00</updated>
    <id>http://dubbs.github.io/blog/2014/09/24/ssl-certificate-for-localhost</id>
    <content type="html"><![CDATA[<p>Create a self-signed certificate and move it to the apache conf
directory.</p>

<pre><code class="sh"># generate private key
openssl genrsa -des3 -out server.key 1024

# generate csr
openssl req -new -key server.key -out server.csr

# remove passphrase from private key
cp server.key server.key.org
openssl rsa -in server.key.org -out server.key

# create self-signed cert 
openssl x509 -req -days 365 -in server.csr -signkey server.key -out server.crt

# move to apache conf dir
cp server.crt /etc/apache2/server.crt
cp server.key /etc/apache2/server.key
</code></pre>

<p>Enable the default ssl configuration, <code>/etc/apache2/httpd.conf</code></p>

<pre><code class="sh"># uncomment the following line
Include /private/etc/apache2/extra/httpd-ssl.conf
</code></pre>

<p>Add host entry for development site, <code>/etc/hosts</code></p>

<pre><code class="sh">127.0.0.1 devsite.local
</code></pre>

<p>Modify the default virtual host entry, <code>/etc/apache2/extra/httpd-ssl.conf</code></p>

<pre><code class="sh"># update config for site
DocumentRoot "/Library/WebServer/Documents/devsite"
ServerName devsite.local:433
</code></pre>

<p>Restart Apache</p>

<pre><code class="sh">sudo apachectl restart
</code></pre>

<p>(Optional) - Add as trusted certificate</p>

<pre><code class="sh">open /etc/apache2/server.crt

# Always trust for all users
# Always trust when using the certificate
</code></pre>

<p>Restart Chrome</p>
]]></content>
  </entry>
  
</feed>
